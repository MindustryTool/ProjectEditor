schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
context: |
   Tech stack: TypeScript, Tanstack Start monorepo setup with pnpm
   Testing: We use vitetest and only do unit testing
   Domain: A web-based Mindustry JSON mod editor that work on both Desktop and Mobile devices
   Style guide: We use Shadcn-ui for our UI components
   I18n: We use i18next for our internationalization
   State management: We use Tanstack Query and Zustand
   Canvas: We use react-konva
   Worker: We use threads.js

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
