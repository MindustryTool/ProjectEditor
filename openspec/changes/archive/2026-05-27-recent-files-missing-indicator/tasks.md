## 1. Implement Missing File Indicator

- [x] 1.1 Update `RecentlyOpenedFilesBar.tsx` — add a `isMissing` check in the map loop using the existing `filePaths` Set
- [x] 1.2 Apply `line-through text-destructive` classNames via `cn()` when the file is missing

## 2. Verify

- [x] 2.1 Run `pnpm typecheck` to confirm no type errors
