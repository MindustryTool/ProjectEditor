## ADDED Requirements

No new capabilities — this change is a pure restructuring: moving UI components from a shared package into their consuming apps. No new functionality is introduced, and no existing spec requirements change.
