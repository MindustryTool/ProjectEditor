## 1. Dependencies

- [x] 1.1 Install `@cloudflare/vite-plugin` and `wrangler` as dev dependencies
- [x] 2.1 Add Cloudflare plugin to `vite.config.ts` before `tanstackStart()`
- [x] 3.1 Create `wrangler.jsonc` with Cloudflare Workers settings
- [x] 4.1 Add `deploy` and `cf-typegen` scripts to `package.json`
