## 1. Add Git Filter

- [x] 1.1 Add `isGitPath` helper and filter to `packages/core/src/importer.ts` — exclude entries under `.git/` and any `.gitignore`/`.gitattributes`/`.gitmodules` files

## 2. Verify

- [x] 2.1 Run `pnpm typecheck` across all packages
